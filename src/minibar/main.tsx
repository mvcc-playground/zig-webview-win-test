import React from "react";
import ReactDOM from "react-dom/client";
import { MiniBarApp } from "./MiniBarApp";
import "./styles.css";

ReactDOM.createRoot(document.getElementById("root")!).render(
  <React.StrictMode>
    <MiniBarApp />
  </React.StrictMode>,
);
