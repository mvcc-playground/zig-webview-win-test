{
  "name": "zig-teste-frontend",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite --host 127.0.0.1 --strictPort --port 5173",
    "build": "vite build",
    "preview": "vite preview --host 127.0.0.1 --strictPort --port 4173",
    "check": "vite build"
  },
  "devDependencies": {
    "vite": "^7.1.3"
  }
}
