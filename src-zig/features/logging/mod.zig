pub const service = @import("service.zig");
pub const LogService = service.LogService;
pub const LogEvent = service.LogEvent;
pub const LogLevel = service.LogLevel;
pub const Config = service.Config;
